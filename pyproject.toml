[project]
name = "roc-cwa-mcp"
version = "0.1.0"
description = "Taiwan CWA Weather API MCP Server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "requests>=2.28.0",
]
